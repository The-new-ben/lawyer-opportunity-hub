# Environment Variables for Production Deployment
# Copy this file to .env.local and fill in the actual values

# Supabase Configuration (Server-side)
SUPABASE_URL="https://mlnwpocuvjnelttvscja.supabase.co"
SUPABASE_SERVICE_ROLE="your-service-role-key"

# Client Configuration
CLIENT_ORIGIN="http://localhost:5173"
PORT=4000

# External Service API Keys (Store in Supabase Secrets for production)
WHATSAPP_PHONE_ID="your-whatsapp-phone-number-id"
WHATSAPP_TOKEN="your-whatsapp-access-token"
OPENAI_API_KEY="sk-..."

# Production Notes:
# 1. Supabase client config is hardcoded in src/integrations/supabase/client.ts
# 2. For production, store secrets in Supabase Dashboard > Edge Functions > Secrets
# 3. Update CLIENT_ORIGIN to your production domain
# 4. Ensure all values are set in your hosting platform's environment variables
