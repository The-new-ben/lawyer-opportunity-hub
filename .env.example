# Environment Variables Configuration
# This file shows required environment variables for different deployment scenarios

# ==================== LOCAL DEVELOPMENT ====================
# For local development, create .env.local with these values

# Supabase Configuration (automatically configured in client.ts)
SUPABASE_URL="https://your-supabase-project.supabase.co"
SUPABASE_ANON_KEY="your-supabase-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"
NETLIFY_AUTH_TOKEN="your-netlify-auth-token"

# Server Configuration (for backend Express server)
CLIENT_ORIGIN="http://localhost:5173"
PORT=4000

# ==================== EXTERNAL API KEYS ====================
# These are stored as Supabase Secrets in production
# Configure them at: https://supabase.com/dashboard/project/mlnwpocuvjnelttvscja/settings/functions

# WhatsApp Cloud API Configuration
WHATSAPP_PHONE_ID="your-whatsapp-phone-number-id"
WHATSAPP_TOKEN="your-whatsapp-access-token"
VITE_WHATSAPP_PHONE_ID="your-whatsapp-phone-number-id"
VITE_WHATSAPP_TOKEN="your-whatsapp-access-token"

VITE_GOOGLE_API_TOKEN="your-google-api-token"
VITE_CALENDLY_TOKEN="your-calendly-token"
VITE_CALENDLY_URL="https://calendly.com/your-link"

# OpenAI API for AI Classification
VITE_OPENAI_API_KEY="sk-your-openai-api-key"

# Payment Processing (Stripe)
STRIPE_PUBLISHABLE_KEY="pk_test_your-stripe-publishable-key"
STRIPE_SECRET_KEY="sk_test_your-stripe-secret-key"
STRIPE_WEBHOOK_SECRET="whsec_your-stripe-webhook-secret"

# ==================== PRODUCTION DEPLOYMENT ====================
# For production hosting (Netlify, Vercel, etc.)
# 1. Set environment variables in your hosting platform dashboard
# 2. Ensure Supabase secrets are configured for Edge Functions
# 3. Update CLIENT_ORIGIN to your production domain
# 4. Use production keys for all external services

# Production domain configuration
PRODUCTION_DOMAIN="https://yourdomain.com"

# ==================== IMPORTANT NOTES ====================
# 1. NEVER commit actual API keys to version control
# 2. Supabase client configuration is handled automatically
# 3. Edge Functions read secrets from Supabase Secrets
# 4. Use different API keys for development and production
# 5. All external API keys should be stored in Supabase Secrets for security
